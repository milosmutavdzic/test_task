import React from 'react';

// - Styles
import './Header.scss';

const Header = () => (
  <header className="app-header">
    <div className="app-header__title">
      <h1>Cities in Serbia</h1>
    </div>
  </header>
);

export default Header;
